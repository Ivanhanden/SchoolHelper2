package com.handen.schoolhelper2.fragments.Timetable;

import com.handen.schoolhelper2.MainActivity;

import java.util.ArrayList;

/**
 * Helper class for providing sample content for user interfaces created by
 * Android template wizards.
 * <p>
 * TODO: Replace all uses of this class before publishing your app.
 */
public class TimetableContent {

    /**
     * An array of sample (Timetable) items.
     */
/*    public static ArrayList<Timetable> ITEMS = new ArrayList<Timetable>();

    static {
        // Add some sample items.
        for (int i = 0; i < MainActivity.timetables.size(); i++) {
            addItem(createTimetable(i));
        }
    }

    public static void addItem(Timetable item) {
        ITEMS.add(item);

    }

    public static Timetable createTimetable(int position) {
        return MainActivity.timetables.get(position);
    }

 */

}
